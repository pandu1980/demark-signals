services:
  - type: web
    name: demark-signals
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn demark_daily_signals:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
